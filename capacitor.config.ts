import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.safehaven.app',
  appName: 'SafeHaven',
  webDir: 'dist',
  backgroundColor: '#F2F2F7'
};

export default config;
